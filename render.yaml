services:
  - type: web
    name: conflict-early-warning
    env: python
    buildCommand: pip install -r requirements-deploy.txt
    startCommand: streamlit run dashboard/app.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0